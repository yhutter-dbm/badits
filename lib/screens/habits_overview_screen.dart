import 'package:flutter/material.dart';

class HabitsOverViewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Habits Overview Screen'),
    ));
  }
}
